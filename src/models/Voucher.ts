export interface Voucher {
    category: string;
    brand: string;
    expired: string;
    oneCoins: number;
    image: string; 
    description: string;
    userManual: string;
    title: string;
}